# Docker-Installation-Script
This repository contains a simple and automated Bash script to install Docker on Linux systems. The script updates the system, installs required dependencies, adds Docker’s official repository, and installs the latest stable version of Docker.

